# Configuración de la aplicación
spring.application.name=constructora
server.port=8080

# Conexión a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/dsi115_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuración de Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML

# Configuración de carga de archivos (para facturas futuras)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB